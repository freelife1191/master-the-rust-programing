install nightly

```shell
rustup toolchain install nightly
```

run

```shell
rustup run nightly rustc --version
```

default nightly

```shell
rustup default nightly
```

rustup update

```shell
rustup update
```